"""The stage: composes every package and runs the lifecycle.

The walking skeleton (fakes + stub controller + scripted agents) arrives at
M0.3; the lifecycle tests live in e2e/tests/ and run in CI forever after.
"""
