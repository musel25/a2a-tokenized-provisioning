"""Published language for a2a-tokenized-provisioning.

The only things two packages may share. Shapes (pydantic models) and ports
(typing.Protocol) arrive at M0.2; until then this package is an empty slot
in the workspace.
"""

V = 0
