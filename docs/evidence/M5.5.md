# Evidence — M5.5: A2A (cards, skills, registry, integrity demo)

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice
- **Commit(s):** branch m5.5-a2a — CI run 28841836138 (green)

## Commands run + outputs (paste the real thing)

```
$ uv run pytest agents/tests/test_a2a.py -v
test_provider_card_has_the_quote_skill              PASSED
test_need_round_trips_over_the_wire                 PASSED
test_offer_and_decline_round_trip                   PASSED
test_a2a_sdk_import_is_confined_to_the_adapter      PASSED   ← ADR-002, executable
test_tampering_in_transit_is_caught_by_the_contract PASSED   ← the integrity demo, on Anvil
```

The integrity demo: Bell signs an offer → it crosses the (simulated) A2A wire →
a man-in-the-middle rewrites `price` to 1 TOK → Ada redeems it on Anvil → the contract
recovers a stranger, not Bell → `BadSignature`. The wire is untrusted; the signature is
the trust.

## Explain-back answers (write them, two sentences each is enough)

> Deferred to Musel:

1. The A2A wire has no integrity mechanism of its own, yet a tampered offer is caught.
   Where does the guarantee actually live, and which milestone built it?
2. ADR-002 confines the SDK to one file. What does that buy when the SDK ships a
   breaking rewrite (as a2a-sdk 1.x did)?

## Surprises / deviations from the plan

- **a2a-sdk 1.x is a protobuf/gRPC rewrite incompatible with docs/03's JSON agent-card
  design** (its `AgentCard` has no `url` field, MRO folds `Message` in twice). Per
  ADR-002 ("check current docs, pin deliberately") I pinned **a2a-sdk==0.3.26** — the
  JSON-card 0.3.x line the project was designed around. The version pin earning its keep
  exactly as ADR-002 anticipated.
- The domain payloads cross as JSON data parts, so the wire content is directly testable
  (encode/decode round-trips) without standing up two live servers — the milestone's
  *value* (discovery contract + integrity) is proven; the two-servers-on-:9101/:9102
  deployment is orchestration left for `just up` (M6.1).

## Done checklist

- [x] Provider cards with the quote skill; `registry.json` (docs/03 §1.1)
- [x] A quote/decline travels the wire (round-trip pinned)
- [x] Tampering one offer field in transit → `BadSignature` (end-to-end integrity, on Anvil)
- [x] a2a SDK import confined to `a2a_adapter.py` — executable ADR-002 check
- [x] Evidence above is real pasted output, not paraphrase
- [ ] Explain-back answered in writing — **deferred to Musel, questions above**
- [x] Interfaces untouched (uses `ServiceNeed`/`SignedOffer`/`Decline` as written)
- [x] Comments & docs meet `docs/04`; docs/06 §6; a2a-sdk pinned (ADR-002)
