# Evidence — M3.4: skeleton v2 (chain+net profile)

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice (short — the seams were already true)
- **Commit(s):** (recorded at close)

## Commands run + outputs (paste the real thing)

The same lifecycle script, three realities:

```
$ uv run pytest e2e/ -q                             # mock — what CI runs forever
17 passed in 0.02s

$ SKELETON_PROFILE=chain uv run pytest e2e/ -q      # skeleton v1: live Anvil
17 passed, 1 warning in 1.23s

$ SKELETON_PROFILE=chain+net uv run pytest e2e/ -q  # skeleton v2: Anvil + real router
17 passed, 1 warning in 2.87s
```

In the chain+net run, `test_happy_path_lifecycle`'s network assertions are read **off
the router**: after activation, `/qos/policer-templates/policer-template[name=a2a-sess-0]`
exists on srl1 with `peak-rate-kbps 50000`; after the controller's `tick()` at chain
time ≥ t1, the config is gone. `test_revocation_tears_down_active_session` does the
same mid-window: Bell's on-chain `revoke(7)` → the watcher fires → the controller
tears down → **the policer disappears from the real router**. That is the M4.5
showpiece's whole mechanism, already true with the stub controller.

## Explain-back answers (write them, two sentences each is enough)

> Deferred to Musel:

1. Between v1 and v2 exactly one method changed per world (`_make_net`) plus two
   read helpers. What property of the M0.3 design made the router swap that small?
2. The chain+net world's `close()` sweeps `a2a-*` policer templates even when a test
   FAILED. What lie would every later run tell if it didn't?

## Surprises / deviations from the plan

- No new gotchas — this slice was pure payoff: rule 7 (the contract suite) and the
  world abstraction absorbed the entire swap. The network assertions moved behind
  `world.provisioned()` / `world.torn_down()` so a recording fake and a real router
  answer the same question; that was the only test edit.
- The zombie-policer sweep (plan's "watch for") lives in `ChainNetWorld.close()` and
  runs from the fixture's finalizer, i.e. also after failures.

## Done checklist

- [x] `SKELETON_PROFILE=chain+net uv run pytest e2e/` green against Anvil + live lab
- [x] Lifecycle leaves a real policer on srl1 and removes it at t1 AND at revocation
- [x] Fixture finalizer sweeps router config even when a test fails
- [x] Evidence above is real pasted output, not paraphrase
- [ ] Explain-back answered in writing — **deferred to Musel, questions above**
- [x] Interfaces untouched
- [x] Comments & docs meet `docs/04`; docs/03c trace note updated for three realities
- [x] Inspection surface: `netctl_explore.ipynb` (M3.2/M3.3) covers the components;
      the profile itself is driven from the terminal (`SKELETON_PROFILE=chain+net …`)
