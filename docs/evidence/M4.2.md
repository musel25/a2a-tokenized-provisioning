# Evidence — M4.2: challenge–response auth

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice
- **Commit(s):** (recorded at close)

## Commands run + outputs (paste the real thing)

```
$ uv run pytest controller/ e2e/ -q
47 passed, 1 warning in 0.27s
   test_replayed_proof_is_E_NONCE_REUSED          ← the plan's headline
   test_never_issued_nonce_is_E_NONCE_REUSED
   test_stale_challenge_is_E_BAD_PROOF            ← judged on CHAIN time
   test_nonce_bound_to_its_ticket
   test_wrong_signer_is_E_NOT_OWNER
   test_garbage_signature_is_E_BAD_PROOF
   test_failed_attempt_burns_the_nonce            ← no probing oracle
   test_controller_id_is_inside_the_signature     ← no cross-controller replay
   test_proof_template_agrees_across_packages     ← e2e: chainmcp == controller, via docs/03
```

## Explain-back answers (write them, two sentences each is enough)

> Deferred to Musel — run notebook §5 first, then:

1. The nonce burns on ANY verification attempt, even a failed one. What attack does
   that close, and what does the honest consumer lose (and how do they recover)?
2. Neither controller nor chainmcp imports the other, yet both build the same proof
   string. Where does their agreement live, and which test would catch drift?

## Surprises / deviations from the plan

- Two refinements beyond the plan's sketch, both pinned by tests: the nonce is **bound
  to its entitlement id** (a challenge for #7 cannot activate #8), and the
  **controller_id inside the signed string** blocks cross-controller replay of an
  otherwise valid proof.
- Error-code mapping settled and documented in docs/05 §4: unknown/reused nonce →
  `E_NONCE_REUSED` (indistinguishable by design), stale or malformed → `E_BAD_PROOF`,
  wrong signer → `E_NOT_OWNER`.
- controller gained `eth-account` (verification crypto only — recover, never sign;
  rule 2 intact). auth.py stays out of domain.py, whose purity test is untouched.

## Done checklist

- [x] Replayed proof rejected (E_NONCE_REUSED) + seven more deny paths, all named
- [x] Cross-package proof-string parity pinned in e2e (import direction respected)
- [x] Evidence above is real pasted output, not paraphrase
- [ ] Explain-back answered in writing — **deferred to Musel, questions above**
- [x] Interfaces untouched (§3.2 implemented as written)
- [x] Comments & docs meet `docs/04`
- [x] Inspection surface: `controller_explore.ipynb` §5 (the dance, live keys in cells)
