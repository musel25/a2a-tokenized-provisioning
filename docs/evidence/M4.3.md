# Evidence — M4.3: translators (golden files)

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice
- **Commit(s):** (recorded at close)

## Commands run + outputs (paste the real thing)

Golden generation (once — then frozen, reviewed, guarded):

```
bandwidth_ticket7.json => [{"method": "apply_bandwidth", "kwargs": {"session_id": "ent7-a1",
  "path": {"device": "srl1", "ingress_if": "ethernet-1/1", "egress_if": "ethernet-1/2"},
  "capacity_bps": 50000000, "qos_class": 1}}]
telemetry_ticket8.json => [{"method": "apply_telemetry", "kwargs": {"session_id": "ent8-a1",
  "target": {"device": "srl1"}, "sensor_paths": ["/interface[name=ethernet-1/1]/statistics"],
  "collector_endpoint": "10.0.0.50:57000", "sample_interval_s": 10}}]
```

```
$ uv run pytest controller/ -q
34 passed in 0.19s
   test_translation_matches_golden[bandwidth] / [telemetry]
   test_unmapped_resource_raises_for_the_wiring_to_map     → E_SCOPE at the edge
   test_calls_are_data_not_behavior                        → method names == the port's
   test_resource_map_loader_rejects_unknown_kind

$ uv run pytest -q
111 passed, 3 warnings in 26.30s
```

## Explain-back answers (write them, two sentences each is enough)

> Deferred to Musel — notebook §6 first, then:

1. Why do the goldens pin the translator output as *files* instead of asserting the
   fields in test code?
2. `resource_map.yaml` is the only place resourceIds meet device names (ADR-005).
   What breaks — architecturally, not mechanically — if netctl learned to read it?

## Surprises / deviations from the plan

- The canonical example grew its telemetry half: `TELEMETRY_ENTITLEMENT_VIEW`
  (ticket #8, node srl1) + `RESOLVED_NODE` joined `a2a_interfaces.fixtures`, and the
  checked-in `resource_map.yaml` carries both canonical entries — story, goldens, and
  the future M6.3 e2e all share them.
- The M0.3 open question ("unmapped resource: E_SCOPE or a new code?") is decided:
  `UnmappedResource` at the translator, mapped to **E_SCOPE** by the wiring — valid
  ticket, wrong venue; no new error code, no `v` bump.
- `ProvisionerCall` stays controller-local (rule 9): the wiring applies it with
  `getattr`, and a test pins that every method name is really on the port.

## Done checklist

- [x] Golden-file tests: fixture entitlement in → exact expected call list out (both types)
- [x] Evidence above is real pasted output, not paraphrase
- [ ] Explain-back answered in writing — **deferred to Musel, questions above**
- [x] Interfaces: fixtures grew canonical telemetry values (no shape change, no `v` bump)
- [x] Comments & docs meet `docs/04`
- [x] Inspection surface: `controller_explore.ipynb` §6 (translate both tickets live)
