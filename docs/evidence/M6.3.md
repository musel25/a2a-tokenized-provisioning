# Evidence — M6.3: telemetry e2e (second serviceType, core reused)

> **Updated 2026-07-07:** telemetry is now the *right to configure telemetry export on the
> device* (ADR-007 revised) — `apply_telemetry` writes a `grpc-tunnel` destination to srl1.
> The "how little changes between service types" result stands and is stronger: both
> translators now emit one device-config write, differing only in subtree.

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice
- **Commit(s):** branch m6.2-6.5-e2e-demo — CI run 28843685054 (green)

## Commands run + outputs (paste the real thing)

The e2e demo continues from bandwidth into telemetry on the SAME code path:

```
$ uv run python -m e2e.demo
  telemetry: 2 samples arrived at the collector     ← srl1 counters → Ada's endpoint (ADR-007)
```

## The thesis result: how little changed (measure and write it down)

Between the bandwidth and telemetry legs of `e2e/demo.py`, what differs:

| Shared (unchanged) | Differs |
|---|---|
| Anvil + settlement contract | the translator (`_bandwidth` → `_telemetry`) |
| the controller: predicate, auth, session machine | one provisioner method (`apply_bandwidth` → `apply_telemetry`) |
| the GnmiProvisioner object (same instance) | the resource map entry (path → node) |
| the DashboardEvent path | — |

The second service type cost **one translator function + one provisioner method** — the
architecture generalizes across products for the price of a translator. That delta is
the result.

## Explain-back answers (deferred to Musel)

1. The telemetry forwarder is process state; the bandwidth policer is on-router config.
   Why does that make their teardowns behave differently across a restart?
2. Name every component that did NOT change between the two service types.

## Done checklist

- [x] Second lifecycle green (telemetry samples reach the collector) — `test_demo.py`
- [x] Core reused: the delta measured and written above
- [x] Evidence above is real pasted output
- [ ] Explain-back — deferred to Musel
- [x] Inspection surface: `e2e/demo.py` (both service types, one run)
