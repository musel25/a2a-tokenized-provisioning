# Evidence — M4.1: controller pure domain (predicate + state machine)

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice
- **Commit(s):** see branch m4.1-controller-domain — CI run 28838989938 (green)

## Commands run + outputs (paste the real thing)

```
$ uv run pytest controller/ -q
....................
20 passed in 0.03s
   every deny path named:  E_NOT_OWNER · E_NOT_STARTED · E_EXPIRED (at end sharp) ·
                           E_REVOKED · E_SCOPE (incl. caller-scoped) · E_CONFLICT
   order pinned:           a request failing EVERYTHING answers E_NOT_OWNER
   machine:                5 legal transitions · terminal teardown absorbed (rule 8) ·
                           illegal moves raise IllegalTransition
   rule 4, executable:     test_domain_purity_no_io_imports — imports ⊆ {__future__, a2a_interfaces}

$ uv run pytest -q
96 passed, 3 warnings in 26.14s        (stub_controller now IMPORTS the domain predicate)
```

## Explain-back answers (write them, two sentences each is enough)

> Deferred to Musel — run `e2e/notebooks/controller_explore.ipynb` first, then:

1. Prove domain.py imports no I/O by listing its imports (the notebook's §4 does it
   live). Why does the controller's *testability* depend on that property?
2. Why is the deny ORDER part of the contract rather than an implementation detail?

## Surprises / deviations from the plan

- The predicate gained a `known_service_types` parameter: "has a translator" is
  caller-dependent by definition — the M0.3 stub translates bandwidth only, the real
  controller both. The stub now delegates to the domain's predicate scoped to `(0,)`;
  exactly one bouncer exists in the codebase.
- The purity rule became a TEST (`ast`-walk over the module's imports), not just an
  explain-back — cheaper than trust, forever.
- Boundary pinned: the window is `[start, end)` — at 16:00:00 sharp the ticket is
  already dead (`test_denies_at_exact_end` guards the `>=`).

## Done checklist

- [x] Unit tests incl. ALL deny paths (named), order pin, machine exhaustive
- [x] Zero I/O imports in domain.py — enforced by an executable test
- [x] Evidence above is real pasted output, not paraphrase
- [ ] Explain-back answered in writing — **deferred to Musel, questions above**
- [x] Interfaces untouched
- [x] Comments & docs meet `docs/04`; docs/05-controller-spec.md opened (phase deliverable)
- [x] Inspection surface: `e2e/notebooks/controller_explore.ipynb` (12 cells, headless-green)
