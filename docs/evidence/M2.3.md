# Evidence — M2.3: Telemetry subscribed by hand (gnmic)

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice (the shortest of Phase 2 — as planned)
- **Commit(s):** (recorded at close)

## Commands run + outputs (paste the real thing)

Install (user-local, no sudo) and first contact:

```
$ gnmic version
version : 0.46.0

$ gnmic -a clab-a2a-srl1:57400 -u admin -p 'NokiaSrl1!' --skip-verify capabilities | head -2
gNMI version: 0.10.0
```

The encoding gotcha, hit and solved (default encoding → hard error):

```
$ gnmic … get --path "/interface[name=ethernet-1/1]/oper-state"
rpc error: … Only ASCII, PROTO/TYPED_VAL, JSON_IETF, … are supported (received encoding: 0)
$ gnmic … -e json_ietf get --path "/interface[name=ethernet-1/1]/oper-state"
"srl_nokia-interfaces:interface/oper-state": "up"
```

The subscription, with iperf3 running hostA→hostB (sample-interval 10 s):

```
$ gnmic -a clab-a2a-srl1:57400 -u admin -p 'NokiaSrl1!' --skip-verify -e json_ietf \
    subscribe --path "/interface[name=ethernet-1/1]/statistics" \
    --mode stream --stream-mode sample --sample-interval 10s
"time": "2026-07-07T04:37:03"   in-octets: 761761034
"time": "2026-07-07T04:37:13"   in-octets: 832824572
"time": "2026-07-07T04:37:23"   in-octets: 904751684
"time": "2026-07-07T04:37:33"   in-octets: 975849124
```

Samples arrive on the 10-second beat; the counter delta reconstructs the traffic:
(832824572 − 761761034) × 8 / 10 = **56.9 Mbit/s** — matching the concurrent iperf run.

## Explain-back answers (write them, two sentences each is enough)

> Deferred to Musel — replay docs/07 §7 first, then:

1. gNMI's three verbs — which one did M2.2 *not* use, which one did M2.3 add, and which
   one is M3.x's whole job?
2. The subscription is "dial-in": who connects to whom, and why does that force the
   ADR-007 decision before `apply_telemetry` can exist?

## Surprises / deviations from the plan

- **SR Linux rejects gnmic's default encoding** — every request needs `-e json_ietf`.
  Recorded in docs/07 §7.2 because M3.1's pygnmi will trip on the same stone.
- TLS is self-signed per lab; `--skip-verify` is the honest lab shortcut (the plan asked
  for exactly this note — docs/07 §7.2).
- Nothing else deviated: install → capabilities → get → subscribe worked first try, and
  the counter arithmetic matched the concurrent iperf reading (56.9 vs ~57 Mbit/s).

## Done checklist

- [x] Samples every 10 s while traffic flows; counter deltas match the iperf stream
- [x] Recipe captured verbatim in docs/07 §7 (install, gotchas, subscribe, the math)
- [x] Evidence above is real pasted output, not paraphrase
- [ ] Explain-back answered in writing — **deferred to Musel, questions above**
- [x] Interfaces untouched
- [x] Comments & docs meet `docs/04`
- [x] Inspection surface: docs/07 §7 (replayable terminal recipe)
