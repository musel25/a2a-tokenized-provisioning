# Evidence — M6.5: demo script + revocation showpiece

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice
- **Commit(s):** branch m6.2-6.5-e2e-demo — CI run 28843685054 (green)

## Commands run + outputs (paste the real thing)

The demo as code (`e2e/demo.py`) — bandwidth then telemetry, one narrated run,
emitting DashboardEvents:

```
$ uv run python -m e2e.demo
a2a demo — bandwidth then telemetry, one code path:
  bandwidth: 100M offered → 49.1 Mbps (policed)
  bandwidth: after teardown → 100.0 Mbps (full)
  telemetry: 2 samples arrived at the collector
measured: {'bandwidth_shaped_mbps': 49.1, 'bandwidth_after_mbps': 100.0, 'telemetry_samples': 2}
```

The revocation finale (the jury-gold moment), proven live — the narrated timeline
captured in M4.5:

```
14:02  session ACTIVE   iperf 100M → 49.3 Mbps received   (policed at 50)
15:10  Bell revoke(7) on-chain — nothing else touched
15:10  controller watcher fired → torn_down
15:10  iperf 100M → 100.0 Mbps received   (policer gone)
```

The written runbook: `docs/08-demo-dashboard.md` (three beats; rehearse cold, twice).

## Explain-back answers (deferred to Musel)

1. The demo's climax needs nothing but `cast send … revoke(7)`. Why is that the most
   convincing single command in the whole system?

## Surprises / deviations

- **The video recording and the cold-rehearsal-twice are inherently manual** — this
  milestone delivers the demo *as runnable code* (`e2e/demo.py`), the *runbook*
  (`docs/08`), and the *proven finale* (M4.5 live tests + timeline). Recording is a
  human sitting down with `just up` and a screen recorder; the script it follows is here.

## Done checklist

- [x] Demo script as code: bandwidth → telemetry → (revocation finale, M4.5)
- [x] Runbook written (docs/08), three beats, cold-replay instructions
- [x] Revocation showpiece proven live (M4.5) with the throughput timeline
- [ ] Recorded twice cold — **manual; the script and stack are ready**
- [x] Evidence above is real pasted output
- [ ] Explain-back — deferred to Musel
- [x] Inspection surface: `e2e/demo.py` + `docs/08` + the dashboard
