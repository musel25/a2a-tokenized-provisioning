# Evidence — M6.2: bandwidth e2e + auto-teardown at t1

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice
- **Commit(s):** branch m6.2-6.5-e2e-demo — CI run 28843685054 (green)

## Commands run + outputs (paste the real thing)

The e2e demo's bandwidth beat (real Anvil + controller + SR Linux lab):

```
$ uv run python -m e2e.demo
  bandwidth: 100M offered → 49.1 Mbps (policed)      ← the plateau, live iperf through srl1
  bandwidth: after teardown → 100.0 Mbps (full)      ← policer removed, full rate returns
```

Auto-teardown at chain-time t1 is proven by the M4.5 showpiece
(`test_controller_showpiece.py::test_expiry_timer_tears_down_at_t1`): the expiry timer
re-reads chain time and tears down the session (and the real policer) at endTime — no
external nudge (ADR-004 in code). The revocation variant kills it mid-window.

```
$ uv run pytest e2e/tests/test_demo.py -q
1 passed        (test_bandwidth_and_telemetry_e2e)
```

## Explain-back answers (deferred to Musel)

1. The plateau number (≈50) lives on a blockchain. Trace the path from `fulfill` to the
   iperf drop — which components does the 50 pass through, and which one enforces it?
2. Auto-teardown is triggered two ways (timer, revocation event). Both re-read the chain
   before acting. Why not trust the trigger itself?

## Surprises / deviations

- The bandwidth e2e is the M2.2 recipe + M4.5 showpiece, packaged as `e2e/demo.py` with
  live iperf and dashboard emission. The ADR-006 shim (missing-ASIC tick) is applied in
  the demo exactly as in M2.2/M3.2.

## Done checklist

- [x] iperf plateau (100M → 49.1 Mbps policed) + full rate after teardown
- [x] Auto-teardown at chain-time t1 (M4.5 expiry timer) and on revocation (showpiece)
- [x] Evidence above is real pasted output
- [ ] Explain-back — deferred to Musel
- [x] Inspection surface: `uv run python -m e2e.demo` + the dashboard
