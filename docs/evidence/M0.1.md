# Evidence — M0.1: Repo scaffold, uv workspace, CI

- **Date:** 2026-06-10
- **Time spent:** ~1 h (including two CI iterations for action version bumps)
- **Commit(s):** `4a0e4f4` (scaffold), `a897d9b` (Node 24 action bump), `61ba0fc` (setup-uv pin)

## Commands run + outputs (paste the real thing)

```
$ uv run pytest -v
============================= test session starts ==============================
platform linux -- Python 3.13.5, pytest-9.0.3, pluggy-1.6.0 -- /home/musel/Github/a2a-tokenized-provisioning/.venv/bin/python
rootdir: /home/musel/Github/a2a-tokenized-provisioning
configfile: pyproject.toml
testpaths: interfaces/tests, e2e/tests
collecting ... collected 2 items

interfaces/tests/test_package.py::test_package_imports_and_declares_schema_version PASSED [ 50%]
e2e/tests/test_stage_exists.py::test_stage_can_see_the_treaty PASSED     [100%]

============================== 2 passed in 0.02s ===============================

$ uv run ruff check .
All checks passed!

$ gh run view 27267654633
✓ m0.1-scaffold ci · 27267654633
JOBS
✓ python in 7s (ID 80528731007)
✓ contracts in 10s (ID 80528731056)
https://github.com/musel25/a2a-tokenized-provisioning/actions/runs/27267654633

$ tree -L 2 (abridged)
CLAUDE.md DESIGN.md README.md Justfile pyproject.toml uv.lock
docs/{00-the-story.md, 01-implementation-plan.md, 03-interfaces.md, adr/, evidence/}
interfaces/{pyproject.toml, src/a2a_interfaces, tests}     ← workspace member
e2e/{pyproject.toml, src/e2e, tests}                       ← workspace member
chainmcp/ netlab/ netctl/ controller/ agents/              ← README stubs only
.github/workflows/ci.yml
```

## Explain-back answers (write them, two sentences each is enough)

1. Why does the controller package never import from `chainmcp` directly — what sits
   between them?
   >
2. What exactly does CI re-run on every push?
   >

## Surprises / deviations from the plan

- GitHub runners force Node 24 from 2026-06-16; `checkout@v4`/`setup-uv@v5` warned as
  deprecated, so we bumped to `checkout@v6` and `setup-uv@v8.2.0` (exact pin — astral
  publishes no `v8` major alias tag yet). First bump attempt (`@v8`) failed CI; the
  failure → exact-pin fix is itself evidence CI works.
- `contracts` CI job guards on `contracts/foundry.toml` existing, so the workflow is
  already shaped for M1.1 without lying green today.
- Workspace members are only `interfaces` + `e2e` (uv errors on missing members); the
  other five packages are README stubs, added to the workspace at their own milestones.
- `just` 1.52.0 installed to `~/.local/bin` via the official installer (not in apt).
- Pre-milestone housekeeping: `00-the-story.md` moved to `docs/` to match every doc's
  references (commit `bc81aff` on main).

## Done checklist

- [x] Tests green locally
- [x] Tests green in CI
- [x] Evidence above is real pasted output, not paraphrase
- [ ] Explain-back answered in writing
- [x] Interfaces untouched (or `v` bumped + docs/03 updated in this commit)
