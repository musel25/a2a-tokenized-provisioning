# Evidence — M5.2: consumer LangGraph graph (tool stubs)

- **Date:** 2026-07-07
- **Time spent:** one autonomous session slice
- **Commit(s):** (recorded at close)

## Commands run + outputs (paste the real thing)

```
$ uv run pytest agents/tests/test_consumer_graph.py -q
2 passed in 0.46s
   test_happy_path_buys_and_activates        transcript: quote→decide→settle→activate→report
   test_decline_exits_gracefully_without_buying  transcript: quote→decide→exit (nothing bought)
```

The graph runs on a stubbed `decide` (an injected fake LLM), so the *structure* is
tested in CI without a model; the one LLM slot swaps a fake for a real `LLMClient` at
runtime. Tools are injected callables (plain stubs now, MCP at M5.4).

## Explain-back answers (write them, two sentences each is enough)

> Deferred to Musel — notebook §2, then:

1. The graph has one conditional edge. Which node does it hang off, and how does a
   `decline` decision reach the exit without touching settlement?
2. Why are the tools a Protocol of injected callables rather than direct chainmcp/
   controller calls inside the nodes?

## Surprises / deviations from the plan

- Nothing surprising — "graphs before MCP" (docs/01 M5.2) meant the tools stay plain
  stubs, so this milestone is pure control flow. The transcript list doubles as the
  "transcript shows the full happy path" the plan asks for.

## Done checklist

- [x] Transcript of a full happy path; a decline exits gracefully
- [x] Evidence above is real pasted output, not paraphrase
- [ ] Explain-back answered in writing — **deferred to Musel, questions above**
- [x] Interfaces untouched
- [x] Comments & docs meet `docs/04`; docs/06 §3
- [x] Inspection surface: `agents_explore.ipynb` §2 (both branches, live transcripts)
